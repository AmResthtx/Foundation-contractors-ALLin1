ANTHROPIC_API_KEY=sk-ant-...
NODE_ENV=production
LOG_LEVEL=info
KB_STRESS_TEST_MODE=true
DB_PATH=./data
WEB3FORMS_KEY=your_key
CRM_EMAIL=ellishuntersmith@gmail.com
CRM_WEBHOOK_URL=optional_n8n_webhook
ENCRYPTION_KEY=will_be_generated
PPI_ALERT_PCT=5
# RSS/Atom feeds for the events watchers (optional). Comma separates sources;
# pipe | separates fallback candidate URLs for one source (first that parses wins).
# LOCAL_FEEDS defaults to HGSD + Houston Public Media environment + Community
# Impact Houston; STATEWIDE_FEEDS to Texas Tribune main + Texas Register weekly.
LOCAL_FEEDS=
STATEWIDE_FEEDS=